mkdir csi_server
cd csi_server

安裝venv

pip install -r requirements.txt

django-admin startproject config .

#! 建立新 app 的 models
python manage.py makemigrations app_name

#! 初始化 Django 內建的資料庫（預設是 SQLite）
python manage.py migrate

#! 刪除 app 做的 migrate
python manage.py migrate app_name zero

#! 所有樣式檔複製到 STATIC_ROOT 中
python manage.py collectstatic --noinput

#! 啟動
python manage.py runserver
python manage.py runserver 0.0.0.0:8000

#! 建立超級使用者
python manage.py createsuperuser

#! 建立 app
python manage.py startapp app_name

#! 終端機開發用
python manage.py shell

#! Django 內建檢查安全性
python manage.py check --deploy

#! 專屬靜態掃描工具
bandit -r . -x ./.venv,./venv,./tests -f txt -o bandit_report.txt

#! Django 專用安全性分析：Safety & Djade
safety check

#! 下載 OCR
winget install UB-Mannheim.TesseractOCR

#! requests 問題
pip install urllib3==1.26.18 charset-normalizer==3.3.2 chardet==5.2.0 --force-reinstall

#! 關閉強制 https (delete 127.0.0.1)
chrome://net-internals/#hsts

#! 圖片去背套件安裝 cpu
pip install "rembg[cpu]"

#* 如果有 NVIDIA 顯卡且配置好 CUDA 環境
pip install "rembg[gpu]"

#! 下載 辨識資料已包含在專案裡 (rtdetrv2_pytorch)
https://github.com/lyuwenyu/RT-DETR

#! 移除單一檔案的追蹤
git rm --cached <檔案路徑>

#! 移除整個資料夾的追蹤 (例如 media 資料夾)
git rm -r --cached <資料夾路徑>

#! 移除目前目錄下所有檔案的追蹤 (注意最後有個點)
git rm -r --cached .

-------docker 相關-------
#! 建立並啟動所有容器，改 Dockerfile、裝新套件
docker compose up --build
#! 建立並啟動所有容器，只改 .py 或 .html 檔
docker compose up -d

#! 徹底移除舊的容器（確保環境變數重載）
docker compose down

#! 建立資料庫表
docker compose exec web python manage.py migrate

#! 建立管理員
docker compose exec web python manage.py createsuperuser

#! 停止並移除容器
docker compose down

#! 容器裡安裝套件
docker compose exec web pip install django-allauth

#! 開發用，可直接看 model 或設定值
docker compose exec web python manage.py shell_plus

#! 清除舊的映象檔
docker rmi csi_server-web


-------ngrok 相關-------
#! 綁定 ngrok_token
./ngrok config add-authtoken 你的TOKEN字串
ngrok config add-authtoken 你的TOKEN字串

#! 啟動 ngrok
./ngrok http 8000 --domain=unjoinable-scribal-vanesa.ngrok-free.dev
ngrok http 8000 --domain=kiwi-loved-slug.ngrok-free.app


------- i18N 相關-------
#! gettext 下載連結，解壓縮後把 D:\gettext\bin 路徑加至 path 裡
https://github.com/vslavik/gettext-tools-windows/releases

#! 產生繁中、英文、日文翻譯檔(裡面要自填)
PYTHONIOENCODING=utf-8 python manage.py makemessages -l zh_Hant -l en -l ja -i ".venv" -i "*.txt" -i "test*"

#! 執行編譯（包含繁中、英文、日文）
python manage.py compilemessages -l zh_Hant -l en -l ja -i .venv


------- 執行排程相關 -------
#! 匯入股票清單
python manage.py init_twse

#! 更新股價資訊
python manage.py update_prices

#! 開一個新的終端機專跑排程
python manage.py start_scheduler


------- yt 相關 -------
winget install Gyan.FFmpeg

winget install OpenJS.NodeJS


------- llm 相關 -------
pip install llama-cpp-python --prefer-binary --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu