# Changelog

此文件記錄 **CSI Portal** 專案的所有重大變更。
版本號遵循 [語義化版本 (SemVer)](https://semver.org/lang/zh-TW/) 規範：
* **Major (X)**：重大架構改動（如更換核心框架）。
* **Minor (Y)**：新增功能。
* **Patch (Z)**：Bug 修復或效能優化。

---

## [1.17.6] - 2026-07-14
### Added (新增)
* **時間未定有興趣行程功能 (Unscheduled/Interested Itinerary)**：
    * 調整行程 `date_time` 欄位為可為空（`null=True, blank=True`），支援在時間未知或待討論時建立行程。
    * 在新增/修改行程 UI (`liff_create.html`) 中新增「時間未定」切換開關，開啟時自動禁用並清空日期時間輸入框。
    * 行程看板頁面 (`liff_list.html`) 新增「🌟 有興趣 / 未定」分頁，專門收錄並渲染時間待定行程卡片。
* **多重活動連結支援 (Multiple Related Links)**：
    * 新增加密 `related_links` 欄位，支援以 JSON 陣列格式儲存多個活動相關 URL。
    * 實作動態的「活動連結編輯面板」，使用者可動態新增/刪除自訂標題與 URL（如地圖、官網等）的連結。
    * 地理定位分享引導時，暫存機制可同步記錄並恢復當前已填寫的多重連結。
    * 行程卡片下方自動將相關活動連結渲染為綠色小連結徽章，點擊直接另開網頁。
* **群組分享、有興趣加入與定案推播系統 (LINE Group Sharing & finalization)**：
    * **新建行程推播**：在群組共享看板中**正常建立新行程**時，LINE 機器人會主動向群組聊天室推送 Flex 訊息通知，附帶標題、類型、時間、地點及發起人資訊。
    * **分享至群組**：時間待定行程卡片新增「分享至群組」按鈕，觸發 `liff.shareTargetPicker` 向好友或 LINE 群組發送設計精美的 Flex Message 邀約卡片。
    * **我有興趣/加入表達**：新增 `api_join_unscheduled_itinerary` 接口。群組成員點選卡片連結進入 LIFF，系統會自動辨識並彈出加入確認，將 LINE 顯示名稱寫入「有興趣成員」列表。
    * **決定時間定案推播**：新增 `api_set_unscheduled_time` 接口。成員在卡片點擊「決定時間」時，可直接指定定案日期，行程將轉為正式行程。同時，**LINE 機器人會主動向群組聊天室發送精美的「行程定案 Flex 通知卡片」**通報大家。
* **一鍵加入 Google 日曆功能 (Add to Google Calendar)**：
    * 看板即將到來行程卡片下方新增「加到日曆」按鈕，自動解析行程時間並包裝生成 Google 日曆新增範本 URL，實現一鍵同步行程。
* **歷史行程隱藏功能 (Hide Historical Itineraries)**：
    * 資料庫模型新增 `is_hidden` 欄位，並實作 `api_hide_itinerary` 控制接口。
    * 歷史紀錄 Tab 清單中的卡片右方新增「👁️‍🗨️ 隱藏（eye-slash）」快捷按鈕，點擊確認後即可將不需要的歷史行程標記為隱藏，使其不顯示於行程列表中。
* **分辨行程個人與群組共用來源 (Distinguish Itinerary Source)**：
    * 後端 API 自動判別 `group_id` 並調用 LINE 官方 API 獲取群組摘要名稱，並快取 24 小時以確保加載效能。
    * 行程看板卡片頂部新增「個人行程」或「群組: [群組名稱]」徽章，供使用者清晰分辨行程來源。
* **全新「探索新番/劇庫」追劇大廳功能**：
    * 追劇看板頁面 (`liff_drama.html`) 新增「🌐 探索新番/劇庫」大廳分頁，呈現資料庫共享之劇庫（支援分頁加載與即時模糊搜尋）。
    * 提供一鍵「➕ 加入清單」功能，並於已加入時自動轉為「已在清單」狀態，簡化個人與共享劇集之同步。
* **分類智慧下拉選單與自訂分類**：
    * 新增/編輯劇集之分類由原本文字欄位改為動態下拉選單，並支援「➕ 自訂新分類...」動態切換為文字框輸入。
    * 提供分類數據接口 `api_get_categories`（使用 POST 方法以防止 ngrok 免費版警告頁面干擾）。
* **同名劇集防重防呆判定**：
    * 在 `api_create_drama` 建立劇集時引入同名防重機制，於記憶體中將加密的劇名解密後進行重複判定，若發現同名劇集則拋出 `400` 錯誤及防重警示。
* **大量新番與劇場版基礎 CSV 自動初始化部署**：
    * 支援在 Django 啟動的 `ready()` 初始化中，透過非同步背景 Thread 讀取基礎 CSV 檔案（包含 2026年7月新番與 2026年日美韓劇、動畫劇場版共 172 部），自動比對資料庫（記憶體內解密比對）將缺漏之影劇建檔，免去生產部署手動匯入的麻煩。

### Fixed & Optimized (修復與優化)
* **修復行程刪除後的列表刷新 Bug**：
    * 將原先在行程刪除成功時無參數調用 `fetchItineraries()` 導致 JavaScript 報錯的 Bug，修正為傳入各自分頁參數同步刷新 `upcoming`、`unscheduled` 與 `history` 列表，提升穩定性。
* **修復排程任務計算提醒時間 Bug (Scheduler NoneType Error)**：
    * 修復在啟動排程 `start_scheduler` 指令掃描行程時，遇到 nullable 之時間待定行程 (`date_time = None`)，因進行 `date_time - timedelta` 運算引發 `unsupported operand type(s) for -` 崩潰的 Bug。修正為預先過濾 `date_time__isnull=False` 行程再行提醒計算。
* **修復手機 LINE 內建瀏覽器（WebKit）請求與跳轉快取 Bug (WebView Cache-Busting)**：
    * 修復 iOS LINE WebView 因相對路徑在 iframe 沙盒或 file:// 環境下 fetch 失敗拋出 `DOMException: The string did not match the expected pattern` 錯誤之 Bug，在前台 JS 引進絕對網域 `apiBaseUrl` 機制。
    * 修復 ngrok 免費版 GET 瀏覽器警告攔截 HTML 導致 JSON 解析崩潰之 Bug，將分類請求全面轉換為 POST 協定以順利穿透。
    * 在跳轉 URL 加入隨機時間戳 `_t`，並在 HTML 與 API 視圖中全面配置強制 Cache-Control 標頭，防止手機端加載舊頁面暫存。
* **防重複 LINE 圖文選單自動化註冊**：
    * 重構圖文選單註冊指令 `register_rich_menu`。Django 啟動時在背景執行緒中自動判定現存選單結構，防止圖文選單重複累積，實現全自動佈署。
* **新增劇集重複自動檢查與清理去重功能**：
    * 解決當多個 Django 進程或重新載入（Reload）執行緒併發執行 `import_base_dramas` 時，因時間差造成同一劇目重複建檔入庫的 Bug。
    * 在匯入命令執行前，新增自動檢查資料庫中同名劇集的去重程式，保留最早建立的原始劇集，並安全地將所有關聯進度與推薦紀錄的外鍵遷移合併，然後刪除其餘重複項。

---

## [1.17.5] - 2026-07-13
### Added (新增)
* **探索派遣支援中途取消 (Dispatch Cancellation)**：
    * 後端新增 `api_cancel_expedition` 接口，允許使用者中途召回探索中的寵物（會清除進度且不發放獎勵）。
    * 前端派遣狀態卡片中加入「取消探索派遣」按鈕，方便玩家隨時靈活調配寵物歸隊。
* **裝飾配件戰鬥能力加成與背景介紹 (Accessory Stat Bonuses & Descriptions)**：
    * 為商城所有服飾配件新增加成能力：草帽 (+10 HP)、皇冠 (+50 HP, +15 ATK)、墨鏡 (+8 ATK)、惡魔角 (+20 ATK, -5 HP)、天使翅膀 (+40 HP, +10 ATK)，並配上生動的故事描述。
    * 配件加成數值直接代入冒險塔回合制自動戰鬥的生命值與攻擊力計算中。
* **寵物能力面板實作 (Pet Status/Ability Panel)**：
    * 主頁面右側新增「寵物能力面板」區塊，呈現目前出戰寵物的生命值 (HP)、攻擊力 (ATK) 及其加成明細，顯示總和戰鬥力 (Power) 與各欄位裝扮狀態。
* **冒險塔怪獸圖鑑實作 (Monster Encyclopedia Panel)**：
    * 新增「怪獸圖鑑」模態框，將爬塔過程中各樓層出現的史萊姆（綠色、毒霧紫色、熔岩紅色、果凍史萊姆王、深淵邪能史萊姆皇）之 HP、ATK 預估值與背景簡介完整收錄展示。
* **寵物進化分支與條件圖鑑 (Evolution Branch Encyclopedia)**：
    * 主頁面新增「進化分支圖鑑」按鈕與模態框，完整展示從蛋、幼年、成長、完熟到終極覺醒的三大性格分支（肥嘟嘟、勇敢、普通）之詳細觸發條件（例如乾糧與奇蹟藥水的餵食比重）與特權屬性加成。

### Fixed & Optimized (修復與優化)
* **修復未出戰寵物培育室渲染崩潰 Bug**：
    * 解決 `PetRenderer` 構造函數中漏掉 `this.ctx` 初始化，導致調用 `this.ctx.clearRect` 拋出 `TypeError` 造成整個 `renderLoop` 崩潰致使未出戰寵物不顯示的 Bug。
    * 為配件圖片 <img> 設置 border 與 outline 為 `none`，並預設設為 `display: "none"`，僅在裝配時改為 `block`，完全杜絕瀏覽器在 `src` 為空時對圖片顯示破圖灰色框線的長年問題。
* **移除培育室點擊跳躍與邊框互動**：
    * 移除培育室畫布容器的點擊監聽器，取消點擊跳躍，並將寵物 DOM 實例設為 `pointer-events: none` 與 `cursor: default`，使培育室寵物純作為背景漫遊且完全不干擾滑鼠點擊，提升網頁互動流暢度。

---

## [1.17.4] - 2026-07-09
### Added (新增)
* **遊戲美術多媒體圖鑑庫版面升級 (5-Column Layout Upgrade)**：
    * 將美術預覽卡片從 4 欄重構升級為 5 欄等寬自適應自折疊佈局（`col-6 col-md`），完整展示「靜態主圖」、「向左走路」、「向右走路」、「熟睡發呆」與「Q彈跳躍」，方便管理員同時對比雙向踏步動態。
    * 修復惡魔翅膀破圖，正名為 **「惡魔角 (pixel_devil_horns.webp)」** 頭飾配件並分配至 `head` 槽位，實作與草帽、皇冠正確的互斥開關對齊邏輯。

### Fixed & Optimized (修復與優化)
* **雙向獨立走路動態載入系統 (Walk Left/Right Splitting)**：
    * **主體防撕裂分流**：走路狀態正式依朝向分流載入 `_walk_left.webp` 與 `_walk_right.webp`，主體改為 `scaleX(1)` 零翻轉，杜絕任何因程式碼粗暴鏡像造成的肢體關節扭曲與怪異割裂。
    * **配件鏡像對齊**：配件圖層單獨套用 `scaleX(dir)` 翻轉，使裝扮在向右走時自動鏡像偏移對齊，插槽偏移量精準貼合。
    * **文字防鏡像優化**：阻斷整個寵物 DOM 容器被鏡像翻轉，將翻轉局限在主體與配件圖層，解決了頭頂寵物名字（nameTag）被左右顛倒鏡像反讀的長年 Bug。
* **縱向非正方形動圖尺寸自適應技術 (Aspect Ratio Auto-Expansion)**：
    * **自然比例維持**：在畫布渲染器 (`pet_renderer.js`)、漫遊桌寵 (`pet_shimeji.js`) 與圖鑑頁面中同步實作長寬比監聽。當偵測到如小綠龍 `baby_dragon_jump.webp`（128x233）等高聳縱向 WebP 動圖時，解鎖容器比例限制，高度等比例拉伸向容器上方延伸、底部對齊。
    * **優化結果**：完全修復手繪高聳跳躍動畫播放時，本體被強行縮小成迷你龍的 Bug，使小龍在跳躍時依舊維持標準的 80px 寬度。

---

## [1.17.3] - 2026-07-08
### Added (新增)
* **新增寵物：烈火幼犬 (PUPPY) 像素系列與動畫整合**：
    * **美術像素化去背**：全新程序化生成 6 款去背透明 WebP 像素精靈，覆蓋幼年體 (`baby_puppy.webp`)、成長體 (`growth_puppy.webp`)、完全體 (`complete_puppy.webp`) 以及三大進化體（`pixel_chubby_puppy.webp`、`pixel_star_puppy.webp`、`pixel_emerald_puppy.webp`）。
    * **隨機盲盒孵化**：更新神祕蛋孵化 API。現在購買神秘寵物蛋孵化時，將會隨機誕生「幻獸綠龍 (DRAGON)」或「烈火幼犬 (PUPPY)」，增加趣味性。
    * **進化分支與回憶錄同步**：實作 `PUPPY_STORIES` 背景故事與小說文字對照表，使個人寵物庫、故事重溫回憶錄能完整呈現犬類故事；且大頭照、爬塔戰鬥精靈及全站漫遊桌寵均能即時同步渲染最新的犬類形態與身上穿戴配件。
* **商城商品一鍵初始化系統 (限管理員)**：
    * 在點數商城頁面上方，新增管理員 (`request.user.is_staff`) 可見的「一鍵建立/初始化寵物商城商品 (限管理員)」按鈕。
    * 點擊確認後自動在後台調用 `api_setup_shop_products` 建立預設寵物商品（神秘寵物蛋、美味寵物乾糧、奇蹟進化藥水），防範重複寫入並極致簡化伺服器部署初始化流程。

### Fixed & Optimized (修復與優化)
* **環境相依套件全線升級與相容性修復**：
    * 更新 `requirements.txt`，將 20 個指定套件（包含 `django` 至 `6.0.7`、`yfinance` 至 `1.5.1`、`yt-dlp` 至 `2026.7.4`、`google-genai` 至 `2.10.0`、`ultralytics` 至 `8.4.90`、`paddleocr` 至 `3.7.0` 等）安全升級至最新穩定版。
    * 為防範依賴版本衝突，特將 `tensorboard` 回退鎖定在 `2.20.0`，以完美相容防禦 DoS 漏洞鎖定的 `protobuf<6.0.0` 版本限制，順利通過 Django 全系統相容性檢測。

## [1.17.2] - 2026-07-08
### Added (新增)
* **培育室多寵物群聚漫遊系統**：
    * **分流設計**：召喚出戰的寵物派往全站網頁漫遊，而所有未出戰的寵物則會全部群聚在培育室 Canvas 中。
    * **多寵物 AI 漫遊**：畫布內的所有未出戰寵物擁有獨立行為決策狀態機，可隨機左右行走、打瞌睡、並能被個別點擊觸發專屬 360 度跳躍與音效。
* **個人寵物庫視覺區隔與全站即時重載**：
    * **高亮框區隔**：個人寵物庫中，出戰寵物頭像顯示「綠色高亮發光框」，未出戰顯示「灰色普通框」，視覺直觀。
    * **無縫更換與同步**：切換出戰寵物或穿脫飾品時，前端即時觸發 `PetShimeji.reload()` 重載，使漫遊桌寵的外觀與配件立即改變，無須重整網頁。

### Fixed & Optimized (修復與優化)
* **全站桌寵拖拉互動優化 (Shimeji Drag & Drop)**：
    * 支援滑鼠與觸控（Touch Events）**上下左右拖曳**桌寵位置，拖曳時播放可愛的掙扎走動動畫。
    * 拖曳放開後將當前釋放高度設定為新的穩定地面（`stableFloor`），在此高度繼續散步與打瞌睡；跳躍後亦會透過重力精準落回該拖曳高度。
* **修復爬塔自動挑戰動畫卡死問題**：
    * 解決多寵物渲染重構導致爬塔找不到 `renderer.petImg` 在第一幀拋出 `TypeError` 崩潰的 Bug。改為在戰鬥引擎中獨立加載出戰寵物與配件，徹底修復自動挑戰卡死、過關層數與金幣未更新的問題。
* **後台多重出戰防禦**：
    * 修改 `views.py` 中的 `api_switch_active_pet`，改採事務原子操作先將該用戶所有其他寵物標記為非出戰狀態 (`is_active=False`)，確保同時僅有一隻寵物出戰，修復多隻寵物同時 active 的安全隱患。
* **Django 各 App 載入效能優化 (啟動時間優化)**：
    * **延遲匯入機制 (Lazy Import)**：經排查，`bionic_chat`、`tigf` 及 `tube_hub` 在 Django 啟動時耗費大量時間（最高達 2.4 秒），主因是在模組頂層導入了 `edge_tts`、`deep_translator` , `pandas` , `openpyxl` , `youtube_transcript_api` 及 `yt_dlp` 等重型第三方函式庫。
    * **效能優化結果**：透過將上述套件改以 **Lazy Proxy** 或 **局部延遲載入**，整體伺服器啟動與熱重載耗時從 **~4.0 秒 縮短至 0.2 秒**，大幅縮減代碼重載延遲，提升開發與伺服器響應效率。

## [1.17.1] - 2026-07-07
### Added (新增)
* **Q 版像素寵物王國遊戲化升級 (v2.0)**：
    * **全站桌寵漫遊 (Shimeji)**：實作全站桌寵漫遊腳本 `pet_shimeji.js`，使用者登入後可在全站網頁右下角召喚像素寵物，進行散步、睡覺、沿兩側邊界攀爬，點擊觸發音效與隨機對話氣泡。
    * **美術像素化 (Pixel Art)**：程序化生成去背透明 WebP 像素精靈圖（包含蛋、幼年、成長、完全、自然/烈焰/肥嘟嘟進化分身及道具配件），大幅減少網頁流量並增強動態表現。
    * **探索派遣系統**：支援 1h/2h/4h/20h 派遣任務，時間截止後領取寶箱，獲得金幣、裝飾配件或奇蹟藥水。
    * **自動爬塔戰鬥**：實作 Canvas 自動回合制戰鬥動畫，寵物與史萊姆撞擊對戰、播放受傷閃爍特效與飄血字，結算獲勝前進下一層並獲得金幣。
    * **金幣商城與配件更衣室**：可用金幣購買「草帽、皇冠、墨鏡、惡魔角、天使翅膀」配件，並於寵物庫更衣室為出戰寵物自由裝配，配件會同步渲染在培育室與全站漫遊上。
    * **肥嘟嘟守護龍進化分支**：若全程無餵食點數商店道具僅靠登入天數升級，Stage 4 將進化為「肥嘟嘟守護龍」，每日登入首簽贈送 50 金幣。
    * **音效管理器擴充**：新增戰鬥受傷 (playHit) 與勝利號角 (playWin) Web Audio API 合成音效。

## [1.17.0] - 2026-07-07
### Added (新增)
* **Q 版寵物培養系統 (pet_system) 與點數商城 (finance) 整合開發**：
    * 新增 2D Q版綠龍五大成長階段插畫與美味寵物乾糧道具素材。
    * 實作個人寵物庫機制，支援擁有多隻寵物、一鍵切換出戰（主畫面跑動）以及在寵物庫重溫已解鎖歷史劇情的「回憶錄」。
    * 實作「視覺小說式劇情播放器」，寵物破殼、進化時全螢幕播放專屬插畫與打字機跑馬燈字幕，右上角支援 Skip 跳過功能，解鎖狀態自動紀錄保存。
    * 使用瀏覽器原生 Web Audio API 開發 8-bit Adorable 程式化合成音效發聲器，支援無延遲的破蛋、餵食、點擊 Q彈跳躍旋轉及進化慶祝音效。
    * 優化點數商城版面，新增一般商品與 Q版寵物專區的 Tab 分類滑動切換，使用微暗黑毛玻璃質感與動畫特效。
    * 支援點數交易與背包系統連動，購買寵物蛋或道具後自動歸入 `UserInventory`。

## [1.16.1] - 2026-07-06
### Added (新增)
* **LINE Bot 定位引導與分頁加載機制優化**：
    * 新增 `🎬 看電影` 活動類型於 Itinerary 選擇欄位、LIFF 建立表單與清單樣式。
    * 實作行程清單資料庫層級的 5 筆分頁載入（SQL Slicing），搭配日期由新至舊（降序）排序，極致優化載入效能。
    * 優化定位流程：於建立行程表單點擊「定位」按鈕，網頁將自動暫存填寫內容並調用 `liff.closeWindow()` 返回聊天室；同時呼叫後端 API 由 LINE Bot 發送步驟引導提醒，避免被 LINE 內嵌瀏覽器安全沙盒阻擋轉址。
    * 當使用者於聊天室完成原生地圖選點發送後，點擊 Flex 卡片即可自動帶入地址並一鍵還原暫存的行程欄位資料。
    * 修改模式下自動隱藏定位按鈕，防範修改行程引導重新建立導致資料錯亂。
    * 調整排程日誌：隱藏 APScheduler 每分鐘重複的執行 INFO 日誌，僅在提醒成功發送或出錯時輸出，且針對群組推播能透過 API 自動抓取群組的真實中文名稱。
    * 機器人進群歡迎語移除已取消開發的語意分析段落，保持版面清爽。

---

## [1.16.0] - 2026-07-03
### Added (新增)
* **LINE Bot 行程管理與個人隱私防護系統**：
    * 建立全新的 line_manager App，實作雙向帳號綁定與加密行程儲存。
    * 支援 Google 帳號合併綁定與歷史行程資料自動轉移、舊臨時 `auth_user` 虛擬帳戶自動安全刪除清理機制。
    * 實作 `GroupMembership` 成員關係動態追蹤與綁定，支援在群組中建立行程自動同步給所有成員。
    * 改版行程看板列表為統一整合個人總表，不再區分群組與個人面板。
    * 實作 LINE LIFF 網頁端面板（行程日曆、手動建立表單、帳密綁定/一鍵新用戶綁定頁面）。
    * 背景排程優化：提醒推播掃描頻率由 5 分鐘縮短至 1 分鐘，並採用時間寬限窗口演算法解決時間時差與競態漏發問題。
    * 於 Django Admin 控制 queryset，限制管理員只能檢視及操作自己建立的行程，且非擁有者無法對加密欄位進行解密，確保隱私。

---

## [1.15.1] - 2026-07-02
### Changed (變更)
* **專案文件與開發指南優化整理**：
    * 重寫 `README.md`，新增整個專案 13 個 App 子應用的模組架構說明，詳細描繪 core, finance, invest, study_brain, vision_brain, media_studio 等模組功能與依賴關係。
    * 補全各 App 的核心技術依賴說明，包括 Gemini, YOLOv11, PaddleOCR, Edge-TTS, rembg, yt-dlp 等。
    * 整理並精煉 Local 環境建立流程、Docker 開發指引、資料庫管理命令與排程更新指令，提升多人協同開發效率。

---

## [1.15.0] - 2026-05-19
### Added (新增)
* **物品轉移系統核心與資料模型**：
    * 建立轉移模組：新增 transfer_system 應用程式，建立 Item (物品) 與 TransferRecord (轉移紀錄) 資料模型，全面套用 Django i18n 多語系並設定繁體中文的 verbose_name。
    * 批次資料匯入：於 Django Admin 後台擴充 CSV 檔案上傳功能，允許管理員快速批次建立物品資料。

* **全域浮動通知與會話快取機制**：
    * 歷史紀錄總覽：建立 transfer_history 作為系統主要入口，提供寬版 Bootstrap 5 表格顯示全系統的轉移歷史，並以對應顏色的 Badge 標示轉移狀態。
    * 彈性取消機制：新增「已取消 (cancelled)」狀態支援，允許管理員在清單頁面針對尚未被處理 (pending) 的請求進行「取消轉移」操作。

* **管理員專屬控制台 (Dashboard)**：
    * 紀錄批次刪除：於清單頁面實作 Checkbox 多選機制與「全選/取消全選」前端邏輯，支援一鍵清理大量辨識紀錄。
    * 連動式實體檔案清理：刪除紀錄時，系統會同步刪除對應的結果圖檔案，並針對原圖進行引用計數檢查，確認無其他紀錄依賴後才移除檔案，確保 media 資料夾的空間健康。

* **個人資產整合顯示**：
    * Profile 頁面擴充：於個人中心頁面 (profile.html) 右側面板新增「My Items」區塊，讓使用者能一目了然目前帳號下實際擁有的所有物品清單與獲取時間。

### Changed (變更)
* **轉移邏輯防呆與驗證升級**：
    * 狀態互斥檢查：優化發起轉移的後端邏輯，自動攔截並阻擋「同一物品同時存在兩筆待處理請求」的情況，確保物品流向與所有權的單一性。
    * 接收者動態排除：前端整合 jQuery 與 Select2，當選擇物品時，自動抓取 data-owner-id 並將接收者下拉選單中的「當前擁有者」選項設為停用 (Disabled)，防止無效轉移。
    * 權限調度解鎖：修改接收者查詢條件，不再強制排除當前操作者，允許管理員 (Admin) 選擇自己作為接收者，實作將他人擁有之物品調度回管理員帳號的業務需求。

* **使用者介面與視覺優化**：
    * 智慧下拉選單：轉移表單全面導入 Select2 套件，為物品與接收者選單增加文字搜尋過濾功能，大幅提升資料量龐大時的選取效率。
    * 獨立佈局重構：在不破壞原 base.html 全域 Flex 佈局的前提下，建立獨立的 .transfer-container 卡片樣式，搭配陰影與圓角設計，將發起轉移與確認接收頁面優化為高質感的置中版面。

---

## [1.14.0] - 2026-04-28
### Added (新增)
* **批次圖片辨識功能**：
    * 支援多檔案上傳：於推論表單實作 multiple 屬性，允許使用者一次選取多張圖片進行批次辨識處理。
    * 自動化批次流：後端採用迴圈邏輯自動處理多筆 ImageRecord 建立與 AI 模型運算，顯著提升大批量檢測的效率。

* **詳情頁「二階段再辨識」機制**：
    * 跨模型比對模組：於紀錄詳情頁新增重新辨識功能。系統會鎖定當前原始圖檔路徑，允許使用者選取其他 AI 模型版本進行二次運算，便於評估不同模型版本間的準確度差異。
    * 原圖共用節能設計：實作資料庫欄位共用邏輯，多筆辨識紀錄可指向同一個實體原始圖檔，有效節省伺服器硬碟空間。
    * 模型重複排除邏輯：於重新辨識選單實作 exclude 篩選，系統會自動隱藏該圖「已使用過」的模型，避免產生重複且無效的辨識數據。

* **批次資料管理系統**：
    * 紀錄批次刪除：於清單頁面實作 Checkbox 多選機制與「全選/取消全選」前端邏輯，支援一鍵清理大量辨識紀錄。
    * 連動式實體檔案清理：刪除紀錄時，系統會同步刪除對應的結果圖檔案，並針對原圖進行引用計數檢查，確認無其他紀錄依賴後才移除檔案，確保 media 資料夾的空間健康。

### Changed (變更)
* **前端視覺架構重構**：
    * 獨立佈局設計：移除對全域 base.html 的依賴，改用專屬的 UI 樣式。首頁優化為左右對稱式分欄佈局（左側操作表單、右側即時結果），提供更直覺的對比體驗。
    * 內容置中優化：清單頁與詳情頁改為窄版內容置中佈局，配合 Bootstrap 風格之卡片設計，提升在寬螢幕環境下的閱讀舒適度。

* **查詢效能與邏輯修復**：
    * 預設時效過濾：優化清單載入邏輯，預設僅顯示「當日 (Today)」之辨識紀錄，減少伺服器初次載入時的查詢壓力。
    * 精準日期範圍查詢：修正 Django 於同一天搜尋時的時區偏移問題。改用 timezone.make_aware 搭配 datetime.combine 產生當日 00:00 至 23:59 的時間區間過濾，確保當日搜尋 100% 準確。

* **靜態資源模組化**：
    * 樣式與腳本解耦：正式將所有專屬樣式抽離至 sh_recognition.css，並將按鈕防呆、全選邏輯、刪除確認等腳本整合至 sh_recognition.js，優化程式碼結構。

---

## [1.13.1] - 2026-04-22
### Added (新增)
* **影片編輯工作室**：
    * 多片段拼接與排序機制：支援一次上傳多個影片檔。透過前端動態生成的片段卡片，使用者可自由透過「▲/▼」按鈕調整影片拼接的先後順序。
    * 視覺化精準裁切：整合 HTML5 影片預覽器，實作「抓取目前時間」功能。使用者可直接在播放器中尋找 A、B 點，一鍵填入開始與結束秒數，大幅提升操作便利性。
    * 專業級編碼控制：導入 ffmpeg-python 作為核心處理引擎。支援自定義輸出品質與多種解析度縮放，並實作 scale 與 pad 濾鏡以確保異質影片拼接時的規格統一。
    * 分離軌道處理邏輯：優化轉碼管線，實作影像與聲音軌道獨立裁切與同步拼接，解決濾鏡套用後可能導致的聲音遺失問題。

* **工作室介面整合優化**：
    * 雙頁籤工作流：實作圖片處理與影片編輯的單頁面整合。使用 Bootstrap Tabs 進行功能切換，並透過隱藏欄位 action 確保後端精準分流處理請求。
    * 動態像素預算系統：於圖片處理表單新增 JavaScript 即時計算邏輯。當使用者選取檔案並調整縮放百分比時，系統將即時換算並顯示「預計輸出解析度」，實現真正的 WYSIWYG。

### Changed (變更)
* **前端資源與效能管理**：
    * JavaScript 模組化抽離：將所有圖片像素計算、影片片段動態產生及 UI 鎖定邏輯移至獨立的 media_studio.js 靜態檔案，提升程式碼可維護性。
    * 多檔案上傳安全規範校正：針對 Django 安全機制，自定義 MultipleFileField 與 MultipleFileInput 類別，成功解決 ClearableFileInput 不支援多檔上傳之 ValueError 異常。

* **UI/UX 感官優化**：
    * 條件式預覽提示：優化處理結果顯示邏輯。僅在解析度產生實質變更時，顯示「原始 vs. 處理後」的像素變化警告框，減少非必要資訊干擾。
    * 非同步狀態視覺化：為影片轉碼流程新增 spinner-grow 動態特效與專屬提示文字（如「影片轉碼中...請稍候」），緩解使用者在長時間運算期間的焦慮感。

---

## [1.13.0] - 2026-04-21
### Added (新增)
* **圖像處理工具**：
    * 多功能圖像壓縮工具：開發全新 media_studio 應用程式，支援上傳圖像進行無損轉換。使用者可自選縮放比例（1% - 100%）與壓縮品質（1 - 100）。
    * AI 智能去背技術：整合 rembg 模組導入 AI 自動去背功能。透過 U2-Net 模型實現一鍵移除背景，並針對 JPEG 輸出自動補強白色背景以防透明失效。
    * 即時數據回饋介面：實作前端預覽與對比機制。處理完成後即時顯示「原始大小 vs. 壓縮後大小」及「原始像素 vs. 縮放後像素」之數據差異。
    * 非同步下載流程：優化使用者體驗，改以 Base64 編碼技術於前端生成下載按鍵，解決直接下載導致的頁面跳轉與狀態丟失問題。

* **WebSocket 通訊基礎架構**：
    * ASGI/Daphne 環境升級：將系統由 WSGI 遷移至 ASGI 架構，於 config.asgi 導入 ProtocolTypeRouter。
    * 即時通訊核心：新增 WebSocket 路由與非同步消費者。成功對接 Godot 遊戲客戶端，實作 JSON 封包雙向傳輸，支援繁體中文 Unicode 解碼顯示。
    * 記憶體通訊層設定：於 settings.py 配置 CHANNEL_LAYERS 記憶體後端，為後續開發奠定基礎。

### Changed (變更)
* **全域導覽列優化**：
    * 於主選單中新增「Image Studio」入口，並套用專屬 text-success 圖示。
    * 優化選單高亮邏輯，確保 media-studio 路徑下能正確觸發 active 狀態。

* **安全性與效能調整**：
    * 調整 INSTALLED_APPS 載入順序，確保 daphne 優先於 staticfiles 以防開發環境下 WebSocket 被攔截。
    * 針對圖像處理視圖實作 JavaScript 鎖定機制，於處理期間禁用「開始壓縮」鍵並顯示 Loading 動態，防止重複提交。

---

## [1.12.0] - 2026-04-13
### Added (新增)
* **多國語系版圖擴張**：
    * 新增日文支援：繼繁體中文與英文後，正式導入日文語系檔。於系統 settings.py 的 LANGUAGES 項目中新增 ja 選項，並完成全站標籤與選單之翻譯對照。
    * i18n 翻譯機制深化：同步更新所有模型與視圖層之 gettext_lazy 標籤，確保企業內部功能在日文介面下能精準顯示對應術語。

* **企業內部資訊 功能模組**：
    * 機密資訊管控系統：針對 CSI Server 專案新增企業專屬儀表板，支援內部公告、規章制度及技術文件管理。
    * 權限隔離機制：實作 is_staff 與企業內部群組權限校驗，確保敏感資訊僅限內部員工存取，防止外部滲透。
    * 模型層擴充：於 models.py 新增 CompanyInfo 等關聯表，並嚴格遵循規範，為所有欄位標記繁體中文 verbose_name。

### Changed (變更)
* **系統預設語系邏輯**：
    * 優化語系切換器 UI，加入日文選項，並維持繁體中文為系統啟動之預設語系。
    * 針對日文長字串，優化前端樣板之 Flexbox 佈局，防止在特定螢幕解析度下產生文字溢出問題。

---

## [1.11.0] - 2026-04-07
### Added (新增)
* **Tube Hub 影音中樞與智慧解析引擎**：
    * 雙軌下載與自動字幕解析：深度整合 yt-dlp 套件，支援從前端直接搜尋並無縫下載 YouTube 資源。系統提供「影片類型」與「音樂類型」雙模式，並透過 youtube_transcript_api 自動抓取原生字幕或進行多國語言的即時翻譯轉換，結合個人筆記打造全方位學習載體。
    * 階層式收藏夾與分類系統：實作高度客製化的個人化資料夾架構，前端採用 Bootstrap 手風琴介面渲染，支援資源在不同資料夾與根目錄間自由移動，讓個人知識庫與影音清單井然有序。

* **Web Audio 即時混音與 KTV 練唱系統**：
    * 純前端即時升降 Key 處理：突破原生 HTML5 限制，於播放器底層導入 Tone.js 函式庫接管音訊路由。利用 Web Audio API 實現即時的音高位移，讓玩家在練唱時能針對伴奏進行正負 12 半音的升降 Key 調整，且完全不影響原曲播放節奏。
    * 專業學習播放控制器：整合 0.5x 至 2.0x 無段語速調整、快捷快進/倒退，以及專為外語學習與歌曲副歌拆解設計的「A-B 段落重複播放」功能，並支援全曲循環，滿足深度練唱與聽力訓練需求。
    * Three.js 3D 沉浸式背景引擎：捨棄傳統 2D CSS 動畫，導入 Three.js 打造立體視角的飄落粒子系統。透過客製化 Shader 材質與物理搖曳運算，在深色背景下營造具備空間縱深感的練唱氛圍。

* **社群共享生態與一鍵收藏機制**：
    * 資源權限動態切換：賦予使用者「公開/私有」的單件資源控制權，可隨時將優質的影片或音樂分享至社群大廳。
    * 零成本檔案共用技術：社群大廳內的公開資源支援「一鍵收藏」。當玩家收藏他人分享的資源時，系統僅會在資料庫建立關聯紀錄，實體影音檔案則完美共用，大幅降低伺服器硬碟負載。

### Changed (變更)
* **實體檔案生命週期與儲存空間最佳化**：
    * 智慧型垃圾回收：重構資源刪除邏輯。當使用者移除個人收藏時，系統會自動核對資料庫的引用狀態。若該實體 .mp4 或 .mp3 檔案已無任何使用者綁定，系統將從後端自動清除該實體檔案，實現伺服器容量的最佳化釋放。
    * 高對比度 UI 與操作防呆：優化深色模式下的視覺焦點，將主要行動按鈕 (如「收藏」) 重構為發光警示色並加入立體懸浮特效。同時在播放器載入時強制監聽全域點擊事件，確保 Web Audio Context 能合法啟動，避免瀏覽器安全性阻擋音頻引擎。

---

## [1.10.1] - 2026-03-31
### Added (新增)
* **全方位 AI 影像視覺中樞**：
    * YOLOv11 物件偵測整合：擴充原有的 OCR 辨識頁面，於 AI 引擎選單新增「YOLOv11」選項，將單純的文字辨識系統正式升級為多模態的視覺分析平台。
    * 即時視覺化標註與統計：當選擇 YOLO 引擎時，後端會自動在圖片上繪製物件辨識方框，並透過 Base64 技術將左側預覽圖無縫替換為「AI 標註影像」。右側文字區塊則會動態生成對齊完善的「物件偵測統計表」，各類物品總數一目了然。
    * 前端防呆與聯動邏輯：導入智慧型表單控制腳本，當使用者切換至物件偵測模式時，系統會自動反灰並鎖定「語言選擇」下拉選單，避免邏輯衝突與無效操作。

* **倖存者遊戲暫停與戰力監控系統**：
    * 全局暫停控制機制：於遊戲畫面右上角置入實體的懸浮暫停按鈕，並支援電腦端玩家使用鍵盤 P 鍵或 Esc 鍵進行快捷操作。暫停時精準凍結所有生怪、位移、子彈與計時器運算。
    * 戰力疊加明細面板：於暫停畫面的覆蓋層中，精心設計了「戰力組成明細表」。系統會在底層嚴格追蹤玩家的「基礎數值」、「局外強化」與「局內升級」，讓玩家隨時掌握當下血量、攻擊、速度等能力的總和來源。

### Changed (變更)
* **原生滿版自適應與行動端觸控優化**：
    * 生 Canvas 響應式渲染：徹底捨棄 CSS Transform Scale 的強制縮放舊思維，改採監聽 window.innerWidth/innerHeight 動態重設畫布真實解析度。完美解決手機直立螢幕導致的字體過小與模糊問題，實現真正的 App 級滿版沉浸體驗。
    * 觸控事件穿透與層級修護：重構暫停選單內的按鈕綁定邏輯，建立封裝的 bindMenuButton 工具函式。透過同時監聽 click 與 touchend 事件，搭配 stopPropagation() 與 z-index 層級提升，徹底解決行動裝置上按鈕點擊無效、或誤觸底層虛擬搖桿的痛點。

* **遊戲底層屬性架構重構**：
    * 移速與攻速獨立運算：精細化「速度」天賦的定義，將籠統的屬性徹底拆分為「移動速度」與「攻擊速度」。
    * 動態冷卻時間系統：重構武器射擊邏輯，攻擊速度改以百分比基準（100% 起跳）進行計算，並動態介入武器冷卻公式（基礎 CD / 攻擊速度比例），讓攻速天賦的升級反饋更加平滑且符合直覺。

---

## [1.10.0] - 2026-03-30
### Added (新增)
* **沉浸式隱藏遊戲中心**：
    * 彩蛋進入機制：於 AI 客服圖示導入「長按 3 秒鎖定」邏輯，需通過密碼驗證方可切換至隱藏遊戲維度。
    * App 化獨立佈局：捨棄傳統網頁導覽列，採用全螢幕黑化沉浸式 UI，並透過 JavaScript 實作 transform: scale 自適應縮放技術，確保在任何解析度下皆能維持 800x600 的黃金遊戲比例。
    * 全域浮動虛擬搖桿：針對行動裝置開發「無形化觸控追蹤系統」，支援畫面上任意位置按下並滑動進行 360 度平滑位移，徹底解決傳統固定搖桿遮擋視線與操作手感不佳的痛點。

* **資料庫驅動的關卡與圖鑑系統**：
    * 動態關卡管理：新增 SurvivorLevel 模型，支援後端即時調整通關時限、生怪頻率、難度倍率與破關獎勵金幣。
    * 擴展性怪物圖鑑：導入 SurvivorMonster 模型，支援自定義怪物屬性（血量、防禦、速度、大小）與小縮圖上傳，並實作 Canvas ctx.clip() 圓形裁切技術，將上傳圖片完美轉化為遊戲敵機頭像。
    * 個人化角色渲染：系統自動串接 Django profile 大頭貼，若玩家已設定個人頭像，將自動取代預設綠點，成為場上唯一的倖存者。

### Changed (變更)
* **Roguelike 隨機升級機制優化**：
    * 三選一隨機天賦池：捨棄固定升級邏輯，改採隨機抽取機制。每次升級由系統自天賦池（補血、攻擊、攻速、雙發機率、移速、血量上限）抽籤產出三個選項，提升每場遊戲的策略多變性。
    * 多重射擊判定系統：重構 autoAttack 邏輯，新增 multishotChance 屬性。當觸發機率判定時，玩家將瞬間噴發第二發具備角度偏移的子彈，大幅強化中後期火力壓制感。

* **遊戲節奏與穩定性修護**：
    * 精準時間校正邏輯：修復過去進入升級或暫停選單後存活時間歸零的 Bug。透過記錄 pauseStartTime 並在返回遊戲時計算 pauseDuration 進行 startTime 平移，確保計時器與生怪節奏完美接續。
    * 效能阻塞防護：優化碰撞偵測演算法，改用距離平方比較以節省 Math.sqrt 計算開銷；並實作 hitCooldown 受傷無敵冷卻與死掉怪物的 filter 強制回收機制，有效杜絕紅球過多導致的瀏覽器卡死現象。
    * 即時互動控制：於畫面右上角置入「懸浮暫停按鈕」，支援 Esc 或 P 鍵快速切換，並同步呼叫後端 API 進行即時金幣結算與存活紀錄存檔。

---

## [1.9.0] - 2026-03-24
### Added (新增)
* **即時 AI 影像辨識中心**：
    * 雙引擎智能切換：全新打造獨立的 OCR 辨識面板，整合 PaddleOCR (超精準) 與 Tesseract (經典版) 雙引擎架構，並支援「中英混合」、「純繁體中文」、「純英文」等多語系模式，精準應對各式複雜版面與文件。
    * 直覺化對照與萃取：前端實作「左圖右文」的流暢預覽介面，結合 FileReader API 達成圖片上傳「零延遲即時預覽」。並貼心新增帶有防呆提示的「一鍵複製」按鈕，讓萃取出的文字無縫銜接使用者的工作流。

---

## [1.8.2] - 2026-03-16
### Added (新增)
* **個人化不重複刷題系統**：
    * 作答軌跡追蹤：擴充 QuizRecord 資料庫模型，新增 attempted_questions 欄位，精準記錄使用者於每份教材中挑戰過的題目歷程。
    * 智慧題庫過濾：進入專屬學習室時，後端會自動比對歷史紀錄並隱藏已作答舊題，確保每次測驗都能專注於全新考點。
    * 破關成就機制：當該份教材的所有題庫皆被刷完時，系統將動態隱藏測驗表單，並渲染專屬的「恭喜破關」獎牌畫面，提供「一鍵呼叫 AI 擴充新題庫」的無縫導引。

* **高自由度學習控制台**：
    * 舊題顯示切換開關：於測驗頁面上方新增精美的切換開關，支援透過 ?show_all=true 網址參數動態重載，將「專注新題」或「全卷複習」的決定權交還給使用者。
    * 視覺化作答標籤：開啟全卷顯示模式時，系統會貼心地在曾經做過的題目旁，標註灰色的「<i class="fas fa-history"></i> 已作答」微型徽章，新舊考題狀態一目了然。

### Changed (變更)
* **無縫接軌的使用者體驗**：
    * 回程車票路由機制：優化 generate_analysis 視圖與前端分頁邏輯。透過隱藏欄位傳遞 next_url 與 ?tab=quiz 參數，解決過去呼叫 AI 擴充題目後被迫跳回首頁的痛點。現在訓練完畢將自動重整，並無縫停留在「練習測驗」頁籤，讓使用者立刻挑戰熱騰騰的新題目。
    * 懸浮置頂進度與強制完卷：重構測驗區塊 UI，導入 position: sticky 打造始終懸浮於畫面頂端的「作答進度面板」（包含總題數、已答、未答）。並實作嚴格的前端防呆邏輯：提交按鈕預設為鎖定反灰狀態，需待「未答題數」精準歸零後方可解鎖交卷。

* **前端架構重構與 API 防護**：
    * 腳本模組化與快取優化：將分散於 HTML 樣板中的 Markdown 即時渲染與互動邏輯，全面抽離並整合至外部的 study_brain.js 中。不僅大幅提升樣板整潔度，更善用瀏覽器快取機制加速網頁載入。
    * 擴充題庫防連點保護：針對耗時較長的「呼叫 AI 擴充新題目」表單導入 showLoadingState 攔截機制。點擊瞬間即刻鎖定觸發與返回按鈕，並替換為旋轉載入動畫，有效杜絕使用者因等待不耐煩而產生連點，確保伺服器穩定並保護 Google API 珍貴配額。

---

## [1.8.1] - 2026-03-13
### Added (新增)
* **混合雙引擎與多模態影音直讀**：
    * 導入 Google File API：全面升級 AI 核心架構，新增支援 .pdf 與 .mp4 / .mov 影音格式。AI 現在能具備「視覺」與「聽覺」，直接讀取 PDF 內的複雜圖表、數學公式，甚至能觀看教學影片、聽取語音並解讀簡報畫面，產出精準考題。
    * Word 純文字引擎解放：針對 .docx 檔案保留高穩定性的純文字萃取模式，並將擷取字數上限從 3 萬字大幅解放至 50 萬字，足以應付數百頁的長篇講義。

* **歷屆考題專屬模式**：
    * 刷題神器機制：於上傳教材表單新增「這是歷屆考題」核取方塊（綁定 Material 模型之 is_exam_paper 欄位）。
    * 智慧任務切換：勾選後系統將動態切換 AI Prompt，省略重點摘要生成，強制要求 AI 專注於提取文件中的考題，並針對無答案的考卷自動運算正解，大幅提升備考效率。

* **AI 錯題詳解與追蹤系統**：
    * 自動生成詳解：擴充資料庫 QuizMistake 模型新增 explanation 欄位。AI 出題時將同步產出「為何正確與為何錯誤」的深度解析。
    * 詳解視覺化介面：測驗結果報表新增專屬的「💡 AI 解析」燈泡區塊，幫助使用者真正理解錯題觀念，而不只是死背答案。
    * 原題號追蹤：錯題標題自動附加原始考卷的題號 (例如：Q3.)，方便使用者快速對照原始教材上下文。

### Changed (變更)
* **系統強健性與底層防呆機制**：
    * 中文檔名編碼防護：為徹底解決 Google GenAI SDK 底層的 ascii 中文路徑編碼報錯 Bug，實作了本機 tempfile 暫存機制。系統會在背景自動生成純英文替身檔案進行上傳，確保各類語言檔名皆能暢通無阻。
    * 雙重閱後即焚：強化隱私與空間管理，檔案交由 AI 解析完畢後，確保 Google 遠端伺服器與本機端的純英文暫存檔皆被徹底刪除，達成零殘留。
    * 攔截器白名單：修正視圖層的空檔案攔截邏輯，將影音格式加入白名單，避免無文字內容的 MP4 遭系統誤擋。

* **使用者體驗 (UI/UX) 與介面優化**：
    * 會員專區收納：重構頂部導覽列，將點數儲值、點數商城、投資儀表板與 AI 教材大腦四大功能，優雅地收納進全新的「會員專區」下拉選單中，並加入動態 Active 狀態與 fa-fw 圖示對齊，維持畫面的清爽專業。
    * 樣板渲染修復：引入 Django {% resetcycle %} 標籤，解決選擇題字母 (A, B, C, D) 跨題連續輪替導致出現 E、F 選項的顯示異常；並優化錯題卡片的 Flexbox CSS，使用 flex-grow-1 確保長文選項不溢出且左右兩欄完美等高。

---

## [1.8.0] - 2026-03-11
### Added (新增)
* **AI 教材大腦與無限擴充題庫**：
    * 智慧文檔解析與生成：全新建立 study_brain 模組，支援上傳 PDF 與 Word 格式教材，並自動萃取文本交由 Google Gemini API產生條理分明的 Markdown 重點摘要與結構化 JSON 選擇題。
    * 題庫無限擴充機制：實作智慧疊加演算法，對同一份教材重複點擊「訓練 AI」時，系統會自動省略摘要生成以節省 Token，並將歷史題目作為黑名單餵給 AI，強制產出全新且具深度的「情境應用題」，動態擴充題庫。

* **沉浸式學習室與追蹤系統**：
    * 互動測驗與結果報表：打造專屬學習室，實作前端選項點擊與作答邏輯，提交後自動結算成績，導向專屬「測驗結果報表」，以視覺化卡片呈現正確率、錯誤率，並提供左紅右綠的錯題比對介面。
    * 個人歷史紀錄儀表板：新增「My Quiz History」頁面，完整列表呈現使用者過去的所有測驗時間、得分與教材來源，支援一鍵點擊重溫歷史錯題，打造完整的學習閉環。

* **全站教材共享與收藏機制**：
    * 社群學習知識庫：打破單機限制，上傳的教材預設為全站公開。儀表板右側新增「我的教材庫」與「探索公開教材」雙頁籤切換功能。
    * 非破壞性收藏邏輯：導入 ManyToManyField 實作 saved_by 收藏機制，使用者可自由將公開教材加入或移出個人圖書館，確保檔案實體與 AI 訓練結果可供全站共享而不被誤刪。

### Changed (變更)
* **系統強健性與 API 防護機制**：
    * AI 伺服器忙碌攔截：優化 utils.py 中的 API 呼叫邏輯，精準捕捉 503 Service Unavailable 與 429 錯誤。捨棄背景無效空轉，改為「快速失敗」並回傳清晰的錯誤訊息，配合 Django messages 與前端 JavaScript alert() 實作即時跳窗提醒。
    * 正向查詢優化：全面修正 Django ORM 查詢邏輯，捨棄依賴 related_name 的動態反向查詢，改採明確的正向過濾，徹底消除靜態型別檢查器的誤判紅線。

* **使用者體驗 (UI/UX) 與介面渲染優化**：
    * Markdown 即時渲染：前端引入 marked.js 引擎，將 AI 產出的純文字 Markdown 標記即時轉換為具備層次感的高質感 HTML 排版（粗體藍字標題、圓點清單）。
    * 懸浮置頂進度條：於測驗頁面導入 position: sticky 與毛玻璃 CSS 特效，讓「已作答/未作答」進度統計條在使用者向下捲動長篇題目時，始終懸浮於畫面頂端。
    * 防呆動畫與原生預覽：實作表單送出攔截機制，點擊「訓練 AI」後按鈕即刻禁用並顯示 Spinner 旋轉動畫，防止重複扣除配額，新增檔案線上預覽端點，支援瀏覽器內嵌分頁直接閱讀 PDF 原檔。

---

## [1.7.0] - 2026-03-09
### Added (新增)
* **投資儀表板與數據視覺化**：
    * 已實現損益與股息追蹤：於 Transaction 模型新增 realized_pnl 欄位，支援賣出價差與現金股息之自動結算，並於儀表板新增「落袋為安」總資產數據卡片。
    * Chart.js 互動圖表整合：實作個人資產配置甜甜圈圖，以及市場數據掃描功能（漲跌幅 Top 10、千金/銅板股 Top 10 柱狀圖）。
    * 即時個股走勢圖 API：新增動態抓取歷史股價之 API 端點，支援前端透過 AJAX 點擊持股代碼或使用搜尋框輸入，即時彈出近 6 個月平滑折線圖。

* **系統自動化排程**：
    * 背景排程器導入：整合 django-apscheduler，實作 BackgroundScheduler 處理每日收盤後 (14:30) 之台股報價自動更新。
    * 平滑中斷機制：主執行緒實作休眠迴圈與 KeyboardInterrupt 捕捉機制，確保終端機可以透過 Ctrl+C 安全關閉排程器而不會假死。

### Changed (變更)
* **報價爬蟲效能與強健性優化**：
    * 極致批量寫入：將千檔股票之迴圈儲存邏輯，全面改寫為記憶體暫存與 bulk_create / bulk_update 批量操作，並配合 select_related 解決 N+1 Query 問題，使資料庫 I/O 時間從數分鐘大幅縮短至 1 秒內。
    * 例外防呆機制：修復 Yahoo Finance 回傳空值 (NaN) 導致的 decimal.ConversionSyntax 錯誤，並加入自動淘汰機制，遇下市或無效代碼時自動標記為 is_active=False，保持資料庫純淨。

* **使用者體驗 (UI/UX) 與介面調整**：
    * 圖表收納機制：為避免頁面過長，運用 Bootstrap Collapse 元件將大盤統計圖表預設隱藏，加入專屬工具列支援一鍵展開與個股快速搜尋。
    * 防呆與動畫：優化 AI 理財規劃表單，送出後按鈕自動禁用並顯示 loading 旋轉動畫，防止使用者連點造成重複發送 API 請求。

---

## [1.6.0] - 2026-03-04
### Added (新增)
* **金流與點數系統**：
    * 綠界科技 (ECPay) 整合：實作線上儲值功能，支援動態 ReturnURL 抓取與 CheckMacValue 簽章驗證機制。
    * 點數商城：開發商品兌換功能，支援「儲值點數」與「紅利點數」混合折抵邏輯，並具備庫存即時扣除機制。
    * 錢包儀表板：於個人檔案整合點數資產總覽（儲值/紅利）與分頁式交易歷史紀錄（每頁 10 筆）。

* **互動體驗 (UI/UX)**：
    * 操作確認機制：商品兌換新增 Bootstrap Modal 二次確認視窗，防止使用者誤觸扣點。
    * 動態回饋彈窗：實作兌換成功後的滿版綠色成功視窗，支援隨意點擊關閉，提升操作儀式感。

### Changed (變更)
* **安定申報比對程式**：
    * 複合標題合併邏輯：支援 Excel 跨行標題自動合併（處理 start_row-3 與 start_row-4），並具備空值自動遞補與底線連接功能。
    * 科學符號 (Scientific Notation) 精度處理：引入 decimal.Decimal 進行高精度比對，並設定相對誤差門檻值 ($1 \times 10^{-5}$)，解決 Excel 長數字四捨五入導致的比對失效問題。
    * 資料正規化：優化日期格式（連字號轉純數字）與字串清洗邏輯，確保資料庫與申報檔欄位一致性。

* **國際化與錯誤修正**：
    * 多國語言補充：新增個人檔案錢包區、交易紀錄表與商城操作相關之繁體中文與英文翻譯。
    * SDK 兼容性修正：調整 ecpay_payment_sdk 呼叫邏輯，改用 generate_check_value 確保本機端與測試網域連線正常。

---

## [1.5.2] - 2026-03-02
### Changed (變更)
* **安定專用功能(後端)**：
    * 公司維度管理架構：重構「安定報表比對」邏輯。系統現在能自動解析申報檔名中的「公司編號」與「報表編號」，並以公司為中心進行二階層式管理。
    * 資料庫動態過濾：比對引擎新增資料清洗邏輯，會自動過濾資料庫檔中 isdel 欄位不為空之無效數據，並精確鎖定特定公司（Cno）的資料列進行比對。

* **安定專用功能(前端)**：
    * 層級式清單：左側導覽導入 Bootstrap 手風琴元件，支援多公司、多報表情境下的收合預覽，大幅提升視覺整潔度。
    * 比對結果強化：比對結果頁面新增「公司編號」標記，並能依據資料一致性自動切換顯示狀態。
    * 快取與穩定性：優化後端 Session Cache 機制，防止多公司併行比對時的資料覆蓋問題，並完成 Model 異動之資料庫遷移。

---

## [1.5.1] - 2026-02-26
### Added (新增)
* **員工專屬知識庫**：於 AISystemSetting 導入 internal_policy 欄位。AI 助理現在能針對公司內部規章如：上班時間、請假規定、加班制度）提供即時諮詢。

### Changed (變更)
* **AI 指令安全優化**：
    * 身分感知(Identity Awareness)：優化系統指令，AI 現在能精確辨識使用者是否具備「公司職員」身分。
    * 資訊防護 (Security Barrier)：強化 Prompt 安全防護，確保公司內部敏感資訊僅對驗證合格的員工開放，避免 GUEST 或一般使用者存取。

---

## [1.5.0] - 2026-02-25
### Added (新增)
* **國際化支援(i18n)**：全站套用多國語言架構，目前支援繁體中文(zh-hant)與英文(en)切換，並可透過導覽列即時切換。

* **信箱驗證功能**：新增非 Google 連結（本地帳號）的電子郵件驗證機制，強化帳號安全性。

* **AI 助理優化**：
    * 支援多國語言偵測，AI 會根據當前介面語系自動切換回覆語言。
    * 新增「打字中」動畫效果，提升使用者等待回應時的互動體驗。

* **UI/UX 增強**：
    * 個人檔案：優化大頭貼上傳介面，隱藏瀏覽器原生組件並套用自訂樣式，支援 i18n。
    * 幸運大轉盤：優化 Excel 匯入介面，支援自訂按鈕樣式與檔名即時顯示，支援 i18n。

### Changed (變更)
* **驗證流程**：更新註冊、登入與登出確認頁面的佈局與翻譯標記。
* **公告系統**：公告列表與詳情頁面全面套用 i18n 標記，支援日期格式本地化。

---

## [1.4.1] - 2026-02-06
### Changed (變更)
* **LOG**：訊息優化。
* **安定**：比對資料功能完善。

---

## [1.4.0] - 2026-02-03
### Added (新增)
* **安定**：新增安定處理頁面。

### Changed (變更)
* **安全性**：全面掃描程式安全性，加強伺服器檢查。

---

## [1.3.0] - 2026-01-30
### Added (新增)
* **發文簿**：新增發文簿取號系統。
* **權限**：新增系統功能權限頁說明。

### Changed (變更)
* **智能客服**：內容根據權限更動回答。

---

## [1.2.0] - 2026-01-29
### Added (新增)
* **大頭貼**：新增個人檔案大頭貼上傳更新。
* **輪盤**：新增抽獎輪盤功能。
* **瀏覽**：統計今日和累計瀏覽人數。
* **背景**：新增雪花、楓葉動態背景。

### Changed (變更)
* **稱呼**：優先顯示暱稱，沒有就顯示帳號名稱。

---

## [1.1.0] - 2026-01-28
### Added (新增)
* **智能客服正式上線**：整合懸浮對話介面與後端 Gemini 3 引擎。
* **RAG 知識庫管理**：新增 `AISystemSetting` 後台資料表，支援免代碼動態調整 AI 系統指令（System Instruction）與學習資料。
* **Markdown 渲染支持**：前端引入 `marked.js`，使 AI 回覆支援標題、列表及粗體排版。

### Fixed (修復)
* **日誌鎖定修復**：導入 `concurrent-log-handler` 徹底解決 Windows 環境下 `WinError 32` 導致伺服器崩潰的問題。

### Changed (變更)
* **SDK 遷移**：從舊版 `google-generativeai` 升級為新版 `google-genai` 套件。
* **註解規範標準化**：
    * Python：統一使用 `#!`。
    * JavaScript：統一使用 `//`。
    * Html：統一使用 `{# #}`。

---

## [1.0.0] - 2026-01-23
### Added (新增)
* **入口網基礎架構**：建置基於 Django 的 CSI Portal 核心框架。
* **開發環境配置**：確立 Python 3.13+ 環境、Ruff 格式化規範及 VS Code 工作區設定。
* **AI 引擎升級**：全面遷移至 **Gemini 3 Flash** (透過 `gemini-flash-latest` 別名)。
* **身分驗證系統**：實作 Google 帳號連動登入、個人檔案管理及 Email 驗證流程。
* **動態公告與工具箱**：開發 Notice 系統與外部工具連結選單，支援從資料庫動態產生成員專區內容。